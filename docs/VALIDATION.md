# 实际验证记录 · 1.0.0-rc.17

2026-09-15 完成完整验证。`npm run verify:release` 退出 0，passed=true、sourceUnchanged=true。源码指纹 `c6cb59fc9ea2c8bc6e1fcfdb47dbf1b07626f6f93c333f0640da9e563c87d8f5`。结果见 [summary.json](validation/1.0.0-rc.17/summary.json)，完整日志见 [verification.log](validation/1.0.0-rc.17/verification.log)。

语法、typecheck、lint、build 均通过。单元 20/20，Harness 自测 9/9，真实隔离 PostgreSQL 集成 122/122，Chromium 150/150，WebKit 150/150，进程故障 8/8，静态守卫 134/134；无跳过、失败或 flaky。`node scripts/prepare-test.mjs` 已先执行，自动化只使用 tome_test 和它的全新恢复目标。

实际离线恢复比较 65 个模型和 9290 个素材文件，数据库内容哈希、原文件哈希、TM 序列高水位和运行维护锁均通过。IngestBatchMember 1214 条、MaterialExport 12 条、MaterialExportEntry 12 条，恢复后完全一致；没有以空表代替恢复验收。

## 本轮改动与回归

范围见 [CATALOG-RC17.md](CATALOG-RC17.md)。搜索按钮与筛选框同排对齐，更多筛选收起后不保留大块留白；排序与视图切换放在结果上方，每页数量与翻页采用 Arco Pagination，手机同样可用。手机页头和批量操作宽度一并修正。后台业务接口、数据库迁移、库存、成本、图片权利和审批规则没有变更。

新增 1440px 和 390px 两条真实交互，各使用 31 件合成商品验证排序、跨页、每页数量即时生效、保留选择、应用待输入筛选及未选品牌阻断。两种浏览器均执行这两条用例。桌面核对搜索按钮与输入框的高度、底边一致；手机核对控件可操作且页面不横向溢出。四张桌面/手机控件和分页截图已实际查看，保存在本机 reports/screenshots。

旧跨页维护测试仅把原按钮定位改为 Arco 的具名分页入口；两件 TM 的真实修改断言保留。未删除或重命名既有用例，未强制点击或刷新页面掩盖渲染问题。已有实写丢回执、关页重开、命令键、原图字节、字典、成交、来源资料等回归完整保留。

开发过程中发现 Arco SelectProps 的 aria-label 类型不接受直接对象字面量；修正类型传递后，typecheck 和上述完整验证通过。定向检查不是完整验证的替代证据。

## 本机业务数据与界面核对

本机应用已更新至 rc.17，地址 `http://127.0.0.1:4318`。旧启动器及所属进程正常停止后，使用原本的本地启动器更新；2 个 API 和 2 个 Worker 正常运行，健康、就绪与网关检查均返回 200。仍使用独立 tome_dev 和既有媒体目录，11 个迁移无待应用项，维护锁保留，外部动作保持关闭。

升级前后只读核对真实 13 件商品、来源关联、图片元数据和成本记录，结果完全一致。39 张原图 SHA256 全部匹配，最小宽度 1500 像素。私人数据快照和检查明细只保存在本机 data，未作为自动化夹具或进入源码包。

已在登录后的内置浏览器查看真实商品库，实际切换最早/最新录入与 30/60 件每页，确认商品顺序和分页控件更新。服务更新后原登录保留，页面显示 13 件商品，并恢复最新录入、30 件每页、图片视图。这里证明本轮实际检查的页面与操作，不等同于经营者已经完成长期使用验收。

## 交付边界

本轮处理商品库搜索、排序、分页及相邻手机布局。导入记录、设置和历史业务页面的原渲染方式继续保留，既有业务与权限回归未缩减。

未执行真实销售、外部采集、平台发布、合作规则激活、AnQiCMS 接入或公网部署。异地共同使用、独立备份与账号配置仍需单独实施；浏览器恢复也不是跨设备草稿同步。

## 源码包

`npm run pack` 退出 0，生成 release/tome-workbench-1.0.0-rc.17.zip。336 个 ZIP 条目中，335 个文件与清单逐项校验，另一项为清单本身；文件字节与当前源码一致，源码指纹与本版完整验证一致。包内没有真实 .env（仅示例）、data、node_modules、会话、密钥或备份目录。经营数据和真实图片保留在本机，不随源码发布。
