# 实际验证记录 · 1.0.0-rc.19

2026-09-15 完成完整验证。npm run verify:release 退出 0，passed=true、sourceUnchanged=true。源码指纹 bf9c63f865037245642e9a14a1f03e5b9457812c1880b1b5a53a8979523fe988。结果见 [summary.json](validation/1.0.0-rc.19/summary.json)，完整日志见 [verification.log](validation/1.0.0-rc.19/verification.log)。

语法、typecheck、lint、build 均通过。单元 20/20，Harness 自测 10/10，真实隔离 PostgreSQL 集成 122/122，Chromium 159/159，WebKit 159/159，进程故障 8/8，静态守卫 133/133；无跳过、失败或 flaky。已先执行 node scripts/prepare-test.mjs，自动化只使用 tome_test 合成数据和全新恢复目标。

实际离线恢复比较 65 个模型和 10630 个素材文件，数据库内容哈希、原文件哈希、TM 序列高水位和运行维护锁均通过。IngestBatchMember 1218 条、MaterialExport 14 条、MaterialExportEntry 14 条，恢复后完全一致。

## 本轮改动与回归

范围见 [PRODUCT-FLOW-RC19.md](PRODUCT-FLOW-RC19.md)。普通商品入口先看详情，明确点编辑才进入表单；从详情编辑保存后回到同一件商品，保留原列表或导入批次。当前页相邻浏览与勾选连续编辑分开。大图支持翻阅、原图读取、缩放及原文件下载。单件录货默认结束，连续录货仍可选；生成资料后直接下载，失败重试同一份资料包。

完成批次直接显示本批已归入商品，清筛选仍保留批次与返回地址。发布审核标签不再被当作商品完整性。暂停用独立确认窗口；库存、询盘、成交和已保存图片的操作边界有明确提示。删除放在详情的「更多」，既有保护与恢复入口保留。后台业务接口、数据库迁移与锁定依赖没有变化，OpenAPI 只更新版本号。

product-library.spec.cjs 新增 7 个用例，在 Chromium/WebKit 分别执行，覆盖查看与编辑往返、取消零写入、原列表条件与相邻商品、1440px/390px 原图浏览、1500×2000 原文件字节、已完成批次范围、单件录货、资料下载失败不重复生成、暂停真实写入丢回执同键恢复及只读权限。旧原图上传、关页恢复、渠道草稿、字典、库存和账务用例继续执行。桌面和手机详情截图已作实际查看，保存在本机 reports/screenshots。

开发回归发现翻图后键盘焦点丢失、筛选提交遗漏批次返回地址，均已修复。首轮完整 Chromium 回归为 156 通过、3 失败：旧删除入口缺少衔接，以及保存后仍要求停在编辑器的旧断言。详情新增「更多」删除入口；测试按新路径进入，同时保留删除保护、历史金额、恢复、请求中禁用和单次写入断言，并核对保存后的详情与数据库字段。3 项定向复测后，重新执行本版完整验证，最终全部通过。未用强制点击、跳过或刷新掩盖错误。

## 本机资料与交付边界

真实资料只做只读界面检查及本机私有核对，不作为自动化夹具。已实际打开真实商品详情、读取 1500×1980 原图、翻图、放大，并从编辑取消返回同一件商品。未提交真实商品文字、图片、库存或财务变更。

本机已通过原本的本地启动器更新至 rc.19，地址 http://127.0.0.1:4318。旧启动器及所属 5 个进程正常停止，使用原有 Compose 项目、tome_dev 和媒体目录重新启动；2 个 API、2 个 Worker 运行。健康与就绪接口均为 200，健康接口返回 1.0.0-rc.19。11 个迁移没有待应用项，未重置数据或绕过维护锁。升级后的内置浏览器登录仍有效，真实详情显示正确且没有可编辑字段和横向溢出。

与本轮重启前私有基线比较，13 件商品、39 条素材元数据、13 条成本，以及各 13 条来源关联、采购关联和导入批次成员均哈希一致。另逐文件读取 39 张真实原图，SHA256 和文件大小全部匹配数据库记录，最小宽度 1500 像素。保留此前人工维护的图片顺序，未恢复旧快照。私有基线、核对脚本和明细只保存在本机 data，未进入源码包。

本轮维持商品库 / 导入记录 / 设置三个主入口。原业务保护继续有效，未执行外部采集、平台发布、真实销售、AnQiCMS 接入或公网部署。合成浏览器用例及本机查看不代表经营者长期使用或双方异地经营验收；浏览器恢复仍不是跨设备草稿同步。

## 源码包

npm run pack 退出 0，生成 release/tome-workbench-1.0.0-rc.19.zip。339 个 ZIP 条目中，338 个文件逐项与清单 SHA256 及当前源码字节核对，另一项为清单本身；源码指纹与本版完整验证一致。包内没有真实 .env（仅示例）、data、node_modules、会话、密钥或备份目录。真实订单、图片、账号资料和本机核对明细不随源码打包。
