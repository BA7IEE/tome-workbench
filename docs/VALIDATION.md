# 实际验证记录 · 1.0.0-rc.18

2026-09-15 完成完整验证。`npm run verify:release` 退出 0，passed=true、sourceUnchanged=true。源码指纹 `219c40b920212bd58232093847b8d19d68521422c11d5f9d4e6298f8aa589223`。结果见 [summary.json](validation/1.0.0-rc.18/summary.json)，完整日志见 [verification.log](validation/1.0.0-rc.18/verification.log)。

语法、typecheck、lint、build 均通过。单元 20/20，Harness 自测 10/10，真实隔离 PostgreSQL 集成 122/122，Chromium 152/152，WebKit 152/152，进程故障 8/8，静态守卫 133/133；无跳过、失败或 flaky。`node scripts/prepare-test.mjs` 已先执行，自动化只使用 tome_test 和全新恢复目标。

实际离线恢复比较 65 个模型和 9776 个素材文件，数据库内容哈希、原文件哈希、TM 序列高水位和运行维护锁均通过。IngestBatchMember 1214 条、MaterialExport 12 条、MaterialExportEntry 12 条，恢复后完全一致；没有以空表代替恢复验收。

## 本轮改动与回归

范围见 [UI-CLEANUP-RC18.md](UI-CLEANUP-RC18.md)。清除六份旧 CSS 及无调用的旧商品字段 HTML、原生行菜单定位。ui08.css 是唯一入口；主题变量、原生控件与 Arco 内部样式分工明确。导入和业务记录搜索区等高对齐，手机标题与按钮分行，原生记录表格保持可读列宽并在内部滚动；采购边框间距、视图选中态及输入焦点正常。货源页手工录货直接复用 QuickIntake。后台业务接口和数据库迁移没有变更，OpenAPI 只更新版本号。

新增 1440px 和 390px 两条逐页检查，各覆盖 25 个登录后路由及登录、展厅，在两种浏览器执行。检查主题、溢出、采购边框间距、搜索控件高度、手机标题按钮位置、设置表格列宽、视图选择和未选品牌阻断，实际点击货源手工录货并关闭返回原页。67 个前端模块均从入口可达，当前本地主题变量没有未定义引用。桌面/手机的导入、货源、设置、采购、商品编辑和登录截图已作针对性查看，保存在本机 reports/screenshots。

开发中的第一轮定向检查暴露原生输入普通边框优先级压过焦点的问题；将类型排除条件放入 :where 后修复。另一轮 WebKit 定向检查在 hash 导航事件执行前误匹配上一页的全部行菜单，定位改为目标 TM 的具名菜单，保留原草稿保护、来源成色和版本断言。新逐页检查等待旧页面卸载后验收目标页。上述问题修复后，最终完整验证全部通过；没有强制点击或用刷新掩盖渲染错误。

旧 studio.css、ux2.css 的文件存在性守卫改为 single-visual-owner 守卫，并新增旧样式导入故障注入自测。全部既有 studio、ux2、原图、字典、库存、成交、批量导入、实写丢回执及关页恢复回归保留。

## 本机运行与真实资料核对

本机已更新至 rc.18，地址 `http://127.0.0.1:4318`。旧启动器及其 5 个所属进程正常停止后，经原本的本地启动器升级；2 个 API、2 个 Worker 正常运行，健康、就绪及网关检查均为 200。使用原有独立 tome_dev 和媒体目录，11 个迁移无待应用项，没有重置数据或绕过维护锁。npm ci 使用现有锁定依赖。

同时发现 4328 仍运行本项目较早的 MVP 交互原型。核对进程命令及目录后已停止该预览服务，4318 保持正常；原型文件保留在本机 data，未作为当前应用或进入源码包。

真实资料基线核对开始时完全一致。最终与旧快照比较发现 7 件商品的 17 个图片顺序值变化；逐条核对 IMAGE_ORDER_CHANGED 审计，均发生于此次本地重启之前，当前顺序与审计中的明确排序完全一致。保留这些同期维护，未恢复旧顺序，也未覆盖旧快照。除这些有审计的图片排序外，13 件商品的字段、来源关联、素材元数据及成本均与基线一致。39 张原图 SHA256 全部匹配，最小宽度 1500 像素；仍为 3 个导入批次、13 条成员证据。私人快照、差异和审计对照仅在本机 data 保存，未成为自动化夹具或进入源码包。原始全量快照比较返回 false 的结果仍保留，不将同期维护写成“所有数据完全未变”。

已在登录后的内置浏览器查看真实商品库、导入记录与设置，核对控件尺寸和表格布局。升级后原登录保留，重新加载的商品库仍显示 13 件、最新录入、30 件每页和图片视图；页面无横向溢出。这些是本次实际页面检查，不等于经营者的长期使用验收。

## 交付边界

本轮统一共享样式并清理发现的旧实现。非商品页保留既有业务渲染器，全部原有权限、原图权利、库存、成本和恢复规则继续有效。没有全站重写为 React，没有修改正式商品业务内容。

未执行外部采集、平台发布、真实销售、合作规则激活、AnQiCMS 接入或公网部署。异地共同使用、独立备份与账号配置仍需单独实施；浏览器恢复不是跨设备草稿同步。

## 源码包

`npm run pack` 退出 0，生成 release/tome-workbench-1.0.0-rc.18.zip。333 个 ZIP 条目中，332 个文件与清单逐项校验，另一项为清单本身；文件字节与当前源码一致，源码指纹与本版完整验证一致。包内没有真实 .env（仅示例）、data、node_modules、会话、密钥或备份目录。经营数据、真实图片和旧原型文件保留在本机，不随源码发布。
