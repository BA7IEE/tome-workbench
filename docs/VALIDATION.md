# 实际验证记录 · 1.0.0-rc.16

2026-09-15 完成完整验证。`npm run verify:release` 退出 0，passed=true、sourceUnchanged=true。源码指纹 `cecd76894a305dcda0f5bbb108ac4ff2652a516ab111d183b939e8139d8bf002`。结果见 [summary.json](validation/1.0.0-rc.16/summary.json)，完整日志见 [verification.log](validation/1.0.0-rc.16/verification.log)。

语法、typecheck、lint、build 均通过。单元 20/20，Harness 自测 9/9，真实隔离 PostgreSQL 集成 122/122，Chromium 148/148，WebKit 148/148，进程故障 8/8，静态守卫 134/134；无跳过、失败或 flaky。`node scripts/prepare-test.mjs` 已先执行，自动化只使用 tome_test 和它的全新恢复目标。

实际离线恢复比较 65 个模型和 8836 个素材文件，数据库内容哈希、原文件哈希、TM 序列高水位和运行维护锁均通过。IngestBatchMember 1214 条、MaterialExport 12 条、MaterialExportEntry 12 条，恢复后完全一致；没有以空表代替恢复验收。

## 本轮改动与回归

范围见 [ARCO-RC16.md](ARCO-RC16.md)。商品库、完整编辑、快速录货/修改字段采用 React + Arco；操作列固定、表格外菜单、批量操作入口与手机布局同步调整。后台业务接口、数据库迁移、库存、成本、图片权利和审批规则没有变更。

新增回归覆盖勾选/弹层切换保留未提交筛选及品牌选择，并发改价后保留介绍草稿、人工合并后仍需主动保存。原有实写丢回执、关页重开、相同命令键、原图字节、字典、成交、来源资料等回归完整保留。

首轮完整执行发现手机卡片宽度、全局字体重置覆盖布局和旧批量入口断言问题；中止后修正。第二轮在桌面截图中发现操作列被横向滚动区隐藏，主动中止并固定操作列；随后完整重跑取得上面的最终结果。两轮中止退出码均为 130，失败与中止日志只保存在本机 data。未删除/重命名旧用例、强制点击或刷新页面掩盖渲染问题。行菜单测试按目标 TM 等待新筛选页，并检查点击前就在视口内，避免自动滚动掩盖不可见入口。

## 本机业务数据与界面核对

正式本机应用已更新至 rc.16，地址 `http://127.0.0.1:4318`，2 个 API 和 2 个 Worker 正常运行。旧进程全部正常停止后，通过原本的本地启动器更新；仍使用独立 tome_dev、既有媒体目录和维护锁，没有待应用的新迁移。健康与就绪检查均通过，外部动作保持关闭。

以本轮升级前快照为准，真实 13 件商品、来源关联、图片元数据和成本记录完全一致。39 张原图 SHA256 全部匹配，最小宽度 1500 像素。私人数据快照和检查明细只保存在本机 data，未作为自动化夹具或进入源码包。

用户登录后，已在内置浏览器检查真实商品库、编辑页、品牌、来源成色与瑕疵说明、三张图片和大图弹层。窄窗口下固定操作列可直接点击，页面没有横向溢出。更新服务后仍保留原登录，显示 rc.16 和 13 件商品；页面停在商品库图片视图。这里只证明本轮实际检查的页面与操作，不等同于经营者已经完成长期使用验收。

## 交付边界

导入记录、设置和历史业务页面仍保留原渲染方式；媒体、资料包、库存与发布等业务控制器通过独立容器继续复用。本轮不是整站 React 重写。

未执行真实销售、外部采集、平台发布、合作规则激活、AnQiCMS 接入或公网部署。异地共同使用、独立备份与账号配置仍需单独实施；浏览器恢复也不是跨设备草稿同步。

## 源码包

`npm run pack` 退出 0，生成 release/tome-workbench-1.0.0-rc.16.zip。332 个 ZIP 条目中，331 个文件与清单逐项校验，另一项为清单本身；文件字节与当前源码一致，源码指纹与本版完整验证一致。包内没有真实 .env（仅示例）、data、node_modules、会话、密钥或备份目录。经营数据和真实图片保留在本机，不随源码发布。
