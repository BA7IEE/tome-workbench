# rc.14 记录页导航排查

确认并修复两项操作问题：从商品或单条待办记录进入后，重置会清掉身份范围及返回路径；查看全部测试记录会回到默认正式数据。现在重置只清筛选，查看全部才解除身份范围，两者保留数据类型和合法返回路径。非商品来源不再错误标注为返回商品工作区。

改动仅在共享记录控件，未改变服务端权限、数据隔离、库存或金融规则，无数据库迁移。三类列表的实际点击与单条询盘内容隔离、待办原筛选返回均纳入两种浏览器。新增测试首次误读创建身份回执中的title，已改为核对创建时明确填写的标题，没有放宽断言。

结果见本版完整validation报告，合成测试不等于真实成交或对外发布。
