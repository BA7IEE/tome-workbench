# 闲鱼分发 Profile

版本：`XIANYU/1.0`。仅适用于 `channel.platform = XIANYU` 的标准交付记录。

- 冻结资料的 `currency` 必须为 `CNY`；没有实时汇率、自动价格引擎或跨渠道金额复制。
- 必须原样保留永久 TM、标题、正文及图片顺序，包括瑕疵图。
- APP 如未提供稳定远端编号，`remoteId` 留空，并在确认说明中写明按永久 TM 的核对依据；
  禁止用 `MANUAL:TM...` 代替。
- 不记录或回传登录状态、Cookie、验证码、设备信息、坐标或页面步骤。
